% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_image.R
\name{plot_image}
\alias{plot_image}
\title{Plot Image with Colony Overlays}
\usage{
plot_image(
  img_df,
  colony_data = NULL,
  show_colony_grid = FALSE,
  use_new_boxes = FALSE,
  show_center_points = FALSE,
  show_rowcol_labels = FALSE
)
}
\arguments{
\item{img_df}{`data.frame`. Image data frame created by `initialize_image()`,
containing pixel coordinates and color values.}

\item{colony_data}{`data.frame` or `NULL`. Colony data with coordinates and
bounding box information. If `NULL`, only the base image is plotted.}

\item{show_colony_grid}{`logical(1)`. If `TRUE`, displays colony bounding
boxes as red rectangles. Default is `FALSE`.}

\item{use_new_boxes}{`logical(1)`. If `TRUE`, uses normalized bounding boxes
(new_xl, new_xr, new_yt, new_yb) from `create_grid_boxes()`. If `FALSE`,
uses original boxes (xl, xr, yt, yb). Default is `FALSE`.}

\item{show_center_points}{`logical(1)`. If `TRUE`, displays colony center
points as yellow dots. Default is `FALSE`.}

\item{show_rowcol_labels}{`logical(1)`. If `TRUE`, displays row-column
labels for each colony. Default is `FALSE`.}
}
\value{
A `ggplot2` object showing the image with selected overlays.
}
\description{
Creates a visualization of an image with optional overlays showing colony
grids, center points, and row-column labels. Flexible display options
allow customization of what colony information to show on top of the
base image.
}
